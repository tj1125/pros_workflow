# Perception + GraspGen Python deps for the AMD ROCm (gfx1150 / gfx1151 iGPU) build.
#
# This file deliberately does NOT list torch / torchvision / torchaudio or the ROCm
# SDK — those come from AMD's per-GPU wheel index and are installed by
# scripts/rocm/setup-rocm-env.sh (or manually, see README "Quick start (AMD ROCm)").
#
# It also omits the CUDA-only packages from get_item_info_agent/requirements.txt
# (xformers / spconv-cu121 / torch_scatter+cuXXX / pytorch3d / moge / sam3d): those
# are only used by the unported legacy SAM3D service (:8006 and :8007 do not need them).
#
# Versions are pinned to the set verified working on gfx1151 (Radeon 8060S).

# --- perception models ---
ultralytics==8.4.51
segment_anything @ git+https://github.com/facebookresearch/segment-anything.git@dca509fe793f601edb92606367a655c15ac00fdf

# --- GraspGen inference (pure-python parts; pointnet2_ops is built separately) ---
diffusers==0.11.1
timm==0.9.16
huggingface-hub==0.25.2
addict==2.4.0
easydict==1.13
safetensors==0.8.0
optree==0.19.1
astor==0.8.1

# --- config / geometry / IO ---
hydra-core==1.3.4
omegaconf==2.3.1
loguru==0.7.3
numpy==2.4.6
scipy==1.17.1
opencv-python==4.13.0.92
trimesh==4.12.2
open3d==0.19.0
plyfile==1.1.4
pyquaternion==0.9.9
imageio==2.37.3

# --- A2A server ---
a2a-sdk[http-server]<1.0
fastapi==0.139.0
uvicorn==0.50.2
python-dotenv==1.0.1

# --- misc ---
PyYAML==6.0.3
tqdm==4.67.3
requests==2.34.2
